# LB Tests for 2026-04-27 — Senior-FE Options 1/2/3

Anchor: `submission_tier1b_greedy_meta.csv` → **LB 0.98094** (current best)
Anchor OOF: 0.98084 (PCR L=0.9955 / M=0.9695 / H=0.9775)

## Recommended LB-probe order (highest expected delta first)

| # | Submission | OOF | OOF Δ vs anchor | PCR direction | test rows differ | Notes |
|--:|:--|:--:|:--:|:--|:--:|:--|
| 1 | `submission_opt1_meta3wnn_iso_a040.csv` | 0.98120 | **+0.00036** | L+0.0001 / M+0.0013 / H-0.0003 | 194 | Option 1 peak. **Top recommendation.** |
| 2 | `submission_opt1_meta3wnn_iso_a030.csv` | 0.98107 | **+0.00023** | L+0.0001 / M+0.0010 / H-0.0004 | 160 | Option 1 LB-validated α (mirrors v1 mechanism). Safer. |
| 3 | `submission_opt1_meta3wnn_iso_a050.csv` | 0.98115 | **+0.00031** | L+0.0001 / M+0.0015 / H-0.0007 | 237 | Option 1 aggressive; matches auto-emit `_a500.csv`. |
| 4 | `submission_tier1b_metastack_meta_3wnn_a500.csv` | 0.98113 | +0.00029 (vs lb3 +0.00052) | — | — | Auto-emit, RAW meta (not iso-cal'd). For iso-vs-raw comparison. |
| 5 | `submission_opt3_col_nndist_colMedium_a070.csv` | 0.98071 | -0.00013 | within guardrail | 80 | Option 3 highest test-flip. Mechanism-novel probe. |
| 6 | `submission_opt2v2_C_NotHtoH_bare_t450.csv` | 0.98074 | -0.00010 | within guardrail | 36 | Option 2 (NN-route) finally with non-zero overrides. |
| 7 | `submission_opt3_col_3way_colMedium_a050.csv` | 0.98076 | -0.00008 | within guardrail | 72 | Option 3 with 3-way OTE Medium column. |

Kept but lower priority (≤30 test diffs OR Δ within noise):
- `submission_opt3_col_nndist_colMedium_a050.csv` (63 diffs, Δ -0.00006)
- `submission_opt3_col_3way_colLow_a070.csv` (61 diffs, Δ ~0)
- `submission_opt2v2_C_NotHtoH_bare_t475.csv` (36 diffs, identical to t450)
- `submission_opt2_route_C_NotHtoH_soft_tH50.csv` (0 test overrides — useless)
- `submission_opt2_route_C_NotHtoH_soft_tH60.csv` (0 test overrides — useless)

## Decision logic per submission

**Option 1 (#1-#4)**: real OOF lift over LB-best 4-stack (+0.00023 to +0.00036). The 3-way OTE
+ NN-distance features added orthogonal signal at the meta-stacker level (errs 8735 vs anchor
9415 = 837 fewer; Jaccard 0.7992 right at the redundancy boundary). PCR trade is favorable
(M up ~0.001 / H down ~0.0004). Probe in α=0.30 → 0.40 → 0.50 order:
- If 0.30 lifts ≥+0.0001 LB, that's the win — STOP probing.
- If 0.30 transfers cleanly (gap close to v1's −0.00010), 0.40 may push another +0.00013.
- If 0.30 regresses, the bank-extension carryover pattern reasserts; STOP, 0.40+0.50 will too.

**Option 3 (#5, #7)**: column-selective blend on Medium. OOF is null (Δ ~0) but mechanism is
structurally novel — never tested before. Probe ONE config to close the lever family. If it
matches LB-best within ±0.0002 it's a closure NULL; if ANY positive surprise that's signal
worth pursuing.

**Option 2 v2 (#6)**: NN-dist override fires on 36 test rows. OOF -0.00010 (sub-noise) so
expected LB ≈ 0.98094 ± 0.0001. Cheap closure probe.

## What we expect

Most likely outcome: **Option 1 α=0.30 ≈ LB 0.98090-0.98115** (real signal at meta-stacker
level, calibration similar to v1's −0.00010 gap). If Option 1 lifts above 0.98114 (current
pack), we've broken the saturation ceiling for the first time since the 16+ confirmations
in CLAUDE.md.

Worst case: bank-extension regression pattern holds (LR v2 was −0.00042; v4 ET+kNN was
−0.00102) and Option 1 lands LB 0.98050-0.98080. Still informative — closes the lever
that "structurally-sound FE additions to meta-stacker bank fail same as weak components".

LB budget: 10/day, none used 2026-04-27. Probe in priority order; STOP after first 2-3
results give clear pattern.

## All artifacts

- `scripts/three_way_keys.py` — 15 (cat × cat × digit) triple builder
- `scripts/nn_distance_features.py` — FAISS k=16 to 10k orig, 5-feat
- `scripts/recipe_full_te.py` — `THREE_WAY_OTE=1` and `NN_DIST_PATH=...` env vars
- `scripts/blend_gate_3way_nn.py` — 4-axis blend gate analyzer
- `scripts/option2_nn_route.py`, `scripts/option2_route_v2.py` — NN-route mechanisms
- `scripts/option3_col_blend.py` — column-selective blend mechanism
- `scripts/lb_ready_emit.py` — orchestrator
- `scripts/tier1b_xgb_metastack.py` — meta-stacker (unchanged + loader fix)
- `scripts/artifacts/oof_xgb_metastack_3wnn.npy` + test (new meta-stacker OOF)
- `scripts/artifacts/oof_recipe_full_te_3way.npy` + test (3-way OTE production OOF)
- `scripts/artifacts/oof_recipe_full_te_nndist.npy` + test (NN-dist production OOF)
- `scripts/artifacts/oof_nn_dist_features.npy` + test (FAISS distance features)
